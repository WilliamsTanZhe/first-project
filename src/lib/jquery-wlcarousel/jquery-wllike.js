

; (function($){
	$.fn.wllike=function(options){
		var defaults={
			width:810,
			height:320,

			// 滚动间隔时间
			duration:3000,

			// 当前索引值（默认0）
			index:0,
			auto:true,// 移入移出是否自动轮播
			btns:true, //是否显示左右按钮
			more:false
		}
		return this.each(function(){
			
		}

	}
})(jQuery);